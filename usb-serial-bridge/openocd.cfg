# --- Probe ---
source [find interface/stlink-v2.cfg]
transport select hla_swd

# Start conservative; increase if stable (e.g., 4000, 8000)
adapter speed 2000

# Common choice when you don't have SRST wired; harmless if you do.
reset_config none

# --- Target ---
source [find target/stm32g4x.cfg]